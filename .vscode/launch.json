{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Backend (Delve)",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/backend",
      "cwd": "${workspaceFolder}/backend",
      "env": {
        "MEDIA_ROOT": "${workspaceFolder}/backend/media",
        "DATA_ROOT": "${workspaceFolder}/backend/data"
      }
    },
    {
      "name": "Frontend (Vite dev server)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --host",
      "cwd": "${workspaceFolder}/frontend"
    }
  ],
  "compounds": [
    {
      "name": "Run mediagrid",
      "configurations": [
        "Backend (Delve)",
        "Frontend (Vite dev server)"
      ],
      "stopAll": true
    }
  ]
}
